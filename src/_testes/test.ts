import { insertPesquisas, insertDummyResponses } from "./insert_fake_data";
import { createDonut, createPesquisaHorizontalBar } from "../libs/SVGDrawn";

const init = async () => {
    //console.log(createDonut('ammout', 800, [10, 30, 40, 20]));

    // console.log(createPesquisaHorizontalBar(600, 40, 20));

}

init()
