


export default {

    set: (valor:string, mensao_id:number, client_id:number) => {

        // verificar se o cliente_id do mensao.id é o mesmo informado
        // salva o valor no hashtags

    },

    del: (hashtag_id:number, mensao_id:number, client_id:number) => {

        // verificar se o cliente_id do mensao.id é o mesmo informado
        // deleta
        
    }

}